# Ensure that no matter what is on the classpath, the tests are run using Embedded mode
# Both remote and embedded can be on the classpath when the tests are executed
# using the Openshift profile

infinispan.embedded.enabled=true
infinispan.embedded.config-xml=infinispan-conf.xml

infinispan.remote.enabled=false

cache.ttl=200